# build
**/bin/
**/obj/
*.user
*.suo
.vs/
# node
landing/node_modules/
# vendored C++ reference runtime (huge; KLIP's runtime is Klip.Engine/Rive in C#)
external/
# binaries & artifacts (o .exe vai como GitHub Release, não no git)
*.exe
*.pdb
*.dll
*.lib
*.mp4
*.zip
# local scratch
scratchpad/
# render/export artifacts
dotnet/_out/
_out/
# OLD python KLIP — apagado por ordem (2026-07-12); não republicar
pc/
*.log
# sem python (KLIP é .NET agora)
*.py
klip.py
pip-*.log
/docs/

# harness de render 3D (ferramenta de dev, nao entra em release)
dotnet/Klip.PbrTest/

# worktrees temporarias dos agentes — nunca entram no repo
.claude/worktrees/
